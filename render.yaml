services:
  - type: web
    name: lf-worldbank-risk-pricing
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m backend.src.http_server
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
