name: GitFlow Guard

on:
  push:
    branches: ["main"]

jobs:
  # O job 'block-direct-main' foi removido em 2026-02-27 por decisão estratégica
  # para permitir push direto na main e acelerar o fluxo de entrega.
  # A proteção de branch via PR approval foi desativada no mesmo período.
  #
  # Manter o arquivo para futuras regras de governança na main.
  
  governance-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder for future main branch governance
        run: |
          echo "✅ Push to main allowed. GitFlow Guard rules are currently disabled."
          echo "Repository: ${{ github.repository }}"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"
